# Nuxt / Nitro SSR on Netlify (auto-detects when NETLIFY=true).
# Do not publish "dist" or SPA-redirect /* to a document function —
# that serves HTML for /_nuxt/*.js and crashes the app.
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
